# Add your additional Hyprland configurations here
#
# This is an additional key binding
# bind = $mainMod CTRL, up, workspace, empty
#
# Example for xwayland
# xwayland {
#   force_zero_scaling = true
# }

# qt5ct environment variable
# envd = QT_QPA_PLATFORMTHEME,qt5ct
debug {
     disable_logs = false
}

source = ~/.config/hypr/conf/keybinds-custom.conf
# moved to uwsm config
# SDL version
# envd = SDL_VIDEODRIVER,wayland
# envd = SDL_VIDEODRIVER,x11
# envd = ELECTRON_OZONE_PLATFORM_HINT,auto
# envd = NIXOS_OZONE_WL,1
# envd = OZONE_PLATFORM,wayland
# envd = QT_QPA_PLATFORM,wayland
# envd = GDK_BACKEND,wayland

# exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# Blur for waybar
#layerrule = blur, waybar

# Uncomment in case you have issues with screensharing
# exec-once = ~/.config/hypr/scripts/xdg.sh

# moved to autostart.conf
# exec-once = dunst
# exec-once = conky -p 3 -c /home/talto/.config/conky/conky.conf
# exec-once = wl-paste --type text --watch cliphist store 
# exec-once = wl-paste --type image --watch cliphist store

# moved to windowrules.conf
# windowrule = float, class:^(Conky)$
# windowrule = float, title:^(syslog)$
# windowrule = float, class:^(teams-for-linux)$
